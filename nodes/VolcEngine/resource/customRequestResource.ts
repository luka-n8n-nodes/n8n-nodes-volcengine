import { ResourceOptions } from '../../help/type/IResource';

const CustomRequestResource: ResourceOptions = {
	name: '自定义请求',
	value: 'customRequest',
	order: 100,
};

export default CustomRequestResource;

